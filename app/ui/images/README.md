# app/ui/images 模块

`app/ui/images/` 保存 fnOS 桌面入口图标。

## 文件约定

- `icon_64.png`：小尺寸入口图标。
- `icon_256.png`：应用中心和高清显示使用的图标。
- 当前图标主题为 iCloud 云朵、双向同步箭头和 NAS 设备，强调“同步到 NAS”的用途。

## 维护注意

- 当前入口配置使用 `images/icon_{0}.png`。
- 如替换图标，保持文件名和尺寸约定不变。
- fnOS 应用中心可能叠加来源角标，这不是图标文件本身的一部分。
